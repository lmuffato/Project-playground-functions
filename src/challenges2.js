// Desafio 10
function techList(tech, name) {
  let techList = [];
  name = 'Lucas';
  let list = {
    techName: tech,
    namePerson: name
  }
  for (let i = 0; i < tech.length; i += 1){
    return list.sort = ((a, b) => (a.techName > b.techName) ? 1 : -1)
  }
  if (tech === 0){
    return 'Vazio!';
  }
}
console.log(techList());

// Desafio 11
function generatePhoneNumber() {
  // seu código aqui
}

// Desafio 12
function triangleCheck() {
  // seu código aqui
}

// Desafio 13
function hydrate() {
  // seu código aqui
}

module.exports = {
  generatePhoneNumber,
  techList,
  hydrate,
  triangleCheck,
};
